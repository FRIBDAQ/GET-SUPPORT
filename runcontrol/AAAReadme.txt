This directory contains simple run control programs;

daqstart - starts a run in a configured CoBo/Asad set.
daqstop  - stops a run in a configured CoBo/AsaD set.

Note that we use the EccClient code from GetController to get a leg up on this.


