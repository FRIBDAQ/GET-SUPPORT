The files in this directory provide a GUI prompter for GET parameters
and a GET data source for the NSCLDAQ ReadoutGUI.
  It is assumed that the bits and pieces of the GET support software are
installed in the user's path.
  The following parameters area available for GET data sources (note this
source does not support pausing runs.

See index.html which is the help file for guidance on the parameters the data
source accepts and requires.

