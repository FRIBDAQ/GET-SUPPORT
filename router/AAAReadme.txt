This directory contains code that provides a version of the data router
that takes GET data and puts it into a ringbuffer.

Note that unfortunately, given the way GET software is constructed, we
need to have modified copies of bits and pieces of the GET software here.

